# Railway configuration for WarZ Tech News Ghost deployment

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
numReplicas = 1
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/ghost/api/admin/site/"
healthcheckTimeout = 300
